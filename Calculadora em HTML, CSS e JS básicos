<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><h1>Calculadora</h1></title>

  <style>
    /* ======== ESTILO GERAL ======== */
    body {
      background: linear-gradient(to bottom right, #042057, #ffffff);
      font-family: 'Poppins', sans-serif;
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      transition: background 0.5s, color 0.5s;
    }

    /* Título */
    h1 {
      margin-bottom: 15px;
      text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
    }

    /* Botão de modo noturno */
    #toggle-dark {
      background-color: #007BFF;
      color: white;
      border: none;
      padding: 10px 15px;
      border-radius: 10px;
      cursor: pointer;
      margin-bottom: 20px;
      transition: 0.3s;
    }

    #toggle-dark:hover {
      background-color: #005FCC;
    }

    /* Caixa da calculadora */
    .calculadora {
      background: white;
      padding: 20px;
      border-radius: 20px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
      width: 280px;
      transition: background 0.5s;
    }

    /* Visor */
    #display {
      width: 100%;
      height: 50px;
      margin-bottom: 15px;
      text-align: right;
      font-size: 1.5em;
      border: none;
      outline: none;
      padding: 10px;
      border-radius: 10px;
      background-color: #C0D5FC;
      color: #004A99;
    }

    /* Área dos botões */
    .botoes {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 10px;
    }

    /* Botões */
    button {
      padding: 15px;
      font-size: 1.3em;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      background-color: #007BFF;
      color: white;
      transition: 0.2s;
    }

    /* Efeito hover */
    button:hover {
      background-color: #2C1AC9;
    }

    /* Botão de limpar */
    #limpar {
      grid-column: span 4;
      background-color: #FF4B4B;
    }

    #limpar:hover {
      background-color: #D63B3B;
    }

    /* Botão de igual */
    #igual {
      background-color: #00B34A;
    }

    #igual:hover {
      background-color: #008F3A;
    }

    /* ======== MODO NOTURNO ======== */
    body.dark-mode {
      background: linear-gradient(to bottom right, #0b0b15, #1b1b2f);
      color: #fff;
    }

    body.dark-mode .calculadora {
      background: #1f1f2e;
    }

    body.dark-mode #display {
      background-color: #333;
      color: #00ffcc;
    }

    body.dark-mode button {
      background-color: #2c3e50;
    }

    body.dark-mode button:hover {
      background-color: #34495e;
    }

    body.dark-mode #limpar {
      background-color: #c0392b;
    }

    body.dark-mode #igual {
      background-color: #27ae60;
    }

    body.dark-mode #toggle-dark {
      background-color: #555;
      color: white;
    }
  </style>
</head>

<body>
  <h1>Calculadora</h1>

  <!-- Botão do modo noturno -->
  <button id="toggle-dark"> Modo Noturno</button>

  <div class="calculadora">
    <input type="text" id="display" readonly>
    <div class="botoes">
      <button>7</button>
      <button>8</button>
      <button>9</button>
      <button>/</button>

      <button>4</button>
      <button>5</button>
      <button>6</button>
      <button>*</button>

      <button>1</button>
      <button>2</button>
      <button>3</button>
      <button>-</button>

      <button>0</button>
      <button>.</button>
      <button id="igual">=</button>
      <button>+</button>

      <button id="limpar">Limpar</button>
    </div>
  </div>

  <script>
    // ======== FUNCIONALIDADE DOS BOTÕES ========
    const display = document.getElementById('display');
    const botoes = document.querySelectorAll('.botoes button');
    const toggleDark = document.getElementById('toggle-dark');

    botoes.forEach(botao => {
      botao.addEventListener('click', () => {
        const valor = botao.textContent;

        if (valor === 'Limpar') {
          display.value = '';
          return;
        }

        if (valor === '=') {
          try {
            display.value = eval(display.value);
          } catch {
            display.value = 'Erro';
          }
          return;
        }

        display.value += valor;
      });
    });

    // ======== SUPORTE AO TECLADO ========
    document.addEventListener('keydown', (event) => {
      const tecla = event.key;

      if (!isNaN(tecla) || ['+', '-', '*', '/', '.'].includes(tecla)) {
        display.value += tecla;
      } else if (tecla === 'Enter') {
        try {
          display.value = eval(display.value);
        } catch {
          display.value = 'Erro';
        }
      } else if (tecla === 'Backspace') {
        display.value = display.value.slice(0, -1);
      } else if (tecla === 'Escape') {
        display.value = '';
      }
    });

    // ======== MODO NOTURNO ========
    toggleDark.addEventListener('click', () => {
      document.body.classList.toggle('dark-mode');

      if (document.body.classList.contains('dark-mode')) {
        toggleDark.textContent = ' Modo Claro';
      } else {
        toggleDark.textContent = ' Modo Noturno';
      }
    });
  </script>
</body>
</html>
